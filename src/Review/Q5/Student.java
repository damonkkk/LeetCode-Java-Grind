package Review.Q5;

public class Student {
    int studentID;
    String name;
    String major;

    // Implement: hashCode() only (no secondaryHashCode needed for linear probing)
}

 class LinearProbingHashTable<K, V> {
     public void add(K p, V i) {
     }
     // Implement: add(), contains(), remove(), get()
    // Use: index = (hash(key) + i) % capacity
}